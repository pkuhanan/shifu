<%= form_with(model: score, local: true) do |form| %>
  <% if score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :score_category_id %>
  </div>

  <div class="field">
    <%= form.label :feedback_id %>
    <%= form.text_field :feedback_id, id: :score_feedback_id %>
  </div>

  <div class="field">
    <%= form.label :points %>
    <%= form.number_field :points, id: :score_points %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
