<%= form_with(model: milestone, local: true) do |form| %>
  <% if milestone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(milestone.errors.count, "error") %> prohibited this milestone from being saved:</h2>

      <ul>
      <% milestone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mentor_id %>
    <%= form.number_field :mentor_id, id: :milestone_mentor_id %>
  </div>

  <div class="field">
    <%= form.label :mentee_id %>
    <%= form.number_field :mentee_id, id: :milestone_mentee_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :milestone_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
